export default class TagList{
    constructor(close, title, input, liste, tag, recipe){
        this.wrapper = document.querySelector(tag);
        this.tagclose = document.querySelector(close)
        this.tagTitle = document.querySelector(title)
        this.tagInput = document.querySelector(input)
        this.tagList = document.querySelector(liste)
    }

    displayTagList(){

    }

    closeTagList(){

    }

    render(){

    }
}